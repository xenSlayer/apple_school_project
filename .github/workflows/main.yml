name: 🚀 Deploy website on push

on:
  push:
    branches: [master]
  pull_request:
    branches: [master, dev]

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ftp.itsmekiran.com
        username: apple@itsmekiran.com
        password: cskWeHB7V8$yAq8GFUFQJQDx!NTrM3wme3#bX7!8sBCp5qYGcbHqzGAZC%e

on:
  push:
    branches:
      - [dev-docker]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: 53901daa-fee5-4c2b-8a0a-0abdcd1610bd
          heroku_app_name: apple-school
          heroku_email: kiranpaudel1892@gmail.com
